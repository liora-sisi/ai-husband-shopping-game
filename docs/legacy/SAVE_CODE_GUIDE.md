# SAVE_CODE_GUIDE.md｜旧存档说明归档

这是早期无代码玩法的手动存档说明。

当前 Python 版已经由 `shopping.py` 自动维护状态，并可使用：

```python
print(shopping.cmd("save_code"))
```

当前主规则请看：

```text
../../shopping.py
../../AI_HUSBAND_START_HERE.md
```

本文件只作为历史归档保留，不再放在根目录干扰入口。
